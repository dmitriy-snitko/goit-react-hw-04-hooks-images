import styled from 'styled-components';
import { color, space, layout, flexbox, border, shadow, position, grid  } from 'styled-system';

export const Box = styled('div')(position, color, space, layout, flexbox, border, shadow, grid);
